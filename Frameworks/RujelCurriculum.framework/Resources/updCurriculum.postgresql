-- Schema updates for Curriculum model in PostgreSQL --


-- v3 (1.0) --

ALTER TABLE CU_REASON
  ADD COLUMN GRADE_NUM smallint,
  ADD COLUMN SECTION_NUM smallint;

INSERT INTO SCHEMA_VERSION (MODEL_NAME,VERSION_NUMBER,VERSION_TITLE)
  VALUES ('Curriculum',3,'1.0');


-- v4 (1.1) --

ALTER TABLE CU_REASON
  DROP COLUMN SCHOOL_NUM;

INSERT INTO SCHEMA_VERSION (MODEL_NAME,VERSION_NUMBER,VERSION_TITLE)
  VALUES ('Curriculum',4,'1.1');
