{
    EOModelVersion = "2.1"; 
    adaptorName = JDBC; 
    entities = (
        {className = EOGenericRecord; name = EOJDBCPrototypes; }, 
        {className = EOGenericRecord; name = Firebird; }, 
        {className = EOGenericRecord; name = MySQL; }, 
        {className = EOGenericRecord; name = PostgreSQL; }
    ); 
}
