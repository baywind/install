{
    attributes = (
        {
            allowsNull = N; 
            columnName = CRITERION; 
            name = criterion; 
            prototypeName = short; 
        }, 
        {columnName = MAX; name = max; prototypeName = short; }, 
        {allowsNull = Y; columnName = WEIGHT; name = weight; prototypeName = short; }, 
        {allowsNull = N; columnName = WORK; name = workID; prototypeName = "int_3"; }
    ); 
    attributesUsedForLocking = (workID, max, weight, criterion); 
    className = EOGenericRecord; 
    classProperties = (work, max, weight, criterion); 
    externalName = "CRITER_MASK"; 
    fetchSpecificationDictionary = {}; 
    name = CriterMask; 
    primaryKeyAttributes = (criterion, workID); 
    relationships = (
        {
            destination = Work; 
            isMandatory = Y; 
            isToMany = N; 
            joinSemantic = EOInnerJoin; 
            joins = ({destinationAttribute = workID; sourceAttribute = workID; }); 
            name = work; 
        }
    ); 
}
