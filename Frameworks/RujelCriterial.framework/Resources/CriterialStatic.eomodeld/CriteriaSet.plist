{
    attributes = (
        {columnName = COMMENT; name = comment; prototypeName = string; }, 
        {columnName = "CS_ID"; name = setID; prototypeName = short; }
    ); 
    attributesUsedForLocking = (comment); 
    className = "net.rujel.criterial.CriteriaSet"; 
    classProperties = (comment, criteria); 
    externalName = "CRIT_SET"; 
    fetchSpecificationDictionary = {}; 
    name = CriteriaSet; 
    primaryKeyAttributes = (setID); 
    relationships = (
        {
            deleteRule = EODeleteRuleCascade; 
            destination = Criterion; 
            isToMany = Y; 
            joinSemantic = EOInnerJoin; 
            joins = ({destinationAttribute = critSet; sourceAttribute = setID; }); 
            name = criteria; 
            ownsDestination = Y; 
            propagatesPrimaryKey = Y; 
        }
    ); 
}
