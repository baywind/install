-- Schema updates for BaseYearly model --

  -- v4 (1.1) --

ALTER TABLE BASE_QUALIFIED_SETTINGS
  ADD COLUMN SECTION_NUM smallint;

CREATE INDEX ON BASE_QUALIFIED_SETTINGS (SETTING_ID);

ALTER TABLE SCHEMA_VERSION
  ALTER COLUMN INSTALL_DATE set DEFAULT CURRENT_TIMESTAMP;

INSERT INTO SCHEMA_VERSION (MODEL_NAME,VERSION_NUMBER,VERSION_TITLE)
  VALUES ('BaseYearly',4,'1.1');