-- Schema updates for Criterial model --

-- v4 (1.0) --

ALTER TABLE CR_WORK_TYPE ADD COLUMN CRITERIA_SET smallint;

UPDATE CR_WORK_TYPE
  SET DFLT_FLAGS = DFLT_FLAGS + 8128
  WHERE DFLT_FLAGS >= 64;

INSERT INTO SCHEMA_VERSION (MODEL_NAME,VERSION_NUMBER,VERSION_TITLE)
  VALUES ('Criterial',4,'1.0');

